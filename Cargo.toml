[package]
name = "system18"
version = "0.1.0"
edition = "2024"

[dependencies]
mail-send = "0.5.2"
tokio = { version = "1", features = ["rt", "macros"] }   # already minimal
anyhow = "1.0"
dotenvy = "0.15"          # for loading .env files, optional but common